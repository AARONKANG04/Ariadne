# Auth0 (optional): for /api/user/node-history and JWT validation
# Use the same domain as frontend (e.g. your-tenant.auth0.com)
# AUTH0_DOMAIN=
# API identifier if you use an Auth0 API (for JWT audience validation)
# AUTH0_AUDIENCE=
# Machine-to-machine app credentials; grant it "update:users" and "read:users"
# AUTH0_M2M_CLIENT_ID=
# AUTH0_M2M_CLIENT_SECRET=
